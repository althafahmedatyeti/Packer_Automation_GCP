
name: Packer GCP Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  packer-build:
    runs-on: ubuntu-latest

    steps:
      # -----------------------------
      # Checkout repository
      # -----------------------------
      - name: Checkout code
        uses: actions/checkout@v4

      # -----------------------------
      # Install Packer
      # -----------------------------
      - name: Install Packer
        run: |
          sudo apt-get update
          sudo apt-get install -y curl unzip gnupg lsb-release

          curl -fsSL https://apt.releases.hashicorp.com/gpg \
            | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg

          echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] \
          https://apt.releases.hashicorp.com $(lsb_release -cs) main" \
          | sudo tee /etc/apt/sources.list.d/hashicorp.list

          sudo apt-get update
          sudo apt-get install -y packer

      # -----------------------------
      # Create GCP credentials file
      # -----------------------------
      - name: Create GCP credentials file
        run: |
          echo '${{ secrets.GCP_SA_KEY }}' > gcp-key.json
          chmod 600 gcp-key.json

      # -----------------------------
      # Packer Init
      # -----------------------------
      - name: Packer Init
        run: |
          packer init packer_template_gcp/gcp-ubuntu.pkr.hcl

      # -----------------------------
      # Packer Validate
      # -----------------------------
      - name: Packer Validate
        run: |
          packer validate packer_template_gcp/gcp-ubuntu.pkr.hcl
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ github.workspace }}/gcp-key.json

      # -----------------------------
      # Packer Build (Create GCP Image)
      # -----------------------------
      - name: Packer Build
        run: |
          packer build \
            -var "admin_password=${{ secrets.ADMIN_PASSWORD }}" \
            -var "user1_password=${{ secrets.USER1_PASSWORD }}" \
            packer_template_gcp/gcp-ubuntu.pkr.hcl
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ github.workspace }}/gcp-key.json
